[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --tb=short
    -q

# Ignore noisy third-party warnings
filterwarnings =
    ignore::DeprecationWarning:pydantic
    ignore::DeprecationWarning:langchain
